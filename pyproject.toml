[project]
name = "supportflow-ai"
version = "0.1.0"
description = "AI-assisted customer support ticket management platform"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
supportflow-ai = "supportflow_ai:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
